@layout('main')

@section('content')
  <a href="/posts" class="">Back</a>
  <a href="/posts/edit/{{ post.id }}" class="btn btn-warning btn-sm ml-3">
    Edit
  </a>
  <form class="d-inline ml-3" action="{{ '/posts/' + post.id + '?_method=DELETE' }}" method="POST">
    {{ csrfField() }}
    <button class="btn btn-danger btn-sm" type="submit">Delete</button>
  </form>
  <hr>
  <h1>{{ post.title }}</h1>
  <p>{{ post.body }}</p>
@endsection